name: "fill in release tags"


# When a release tag is pushed (i.e v2.3.0), we can update shortened tags
on:
  release:
    types: [ "released" ]

jobs:
  fill-tags:
    uses: "KyoriPowered/.github/.github/workflows/shared-action-fill-in-tags.yaml@7b79b090212a9c8e371b71c23f5e7b097d125997" # trunk
    secrets: "inherit"
