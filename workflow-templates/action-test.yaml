name: Test

on: push

jobs:
  test:
    uses: "KyoriPowered/.github/.github/workflows/shared-action-test.yaml@7b79b090212a9c8e371b71c23f5e7b097d125997" # trunk
    secrets: "inherit"

  # change me!
  functional-test:
    runs-on: "ubuntu-latest"
    steps:
    - name: "checkout"
      uses: "actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803" # v6.1.0
    - name: "Run functional test"
      uses: ./
      id: test-run
      run: |
        echo "::error title=Functional test not configured::Test the action here!"
        exit 1
