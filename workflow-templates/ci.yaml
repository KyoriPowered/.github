name: "build"

on:
  push:
    branches: ["**"]
    tags-ignore: ["**"]
  pull_request:
  release:
    types: [released]

jobs:
  call-build:
    uses: "KyoriPowered/.github/.github/workflows/shared-ci.yaml@7b79b090212a9c8e371b71c23f5e7b097d125997" # trunk
    secrets: "inherit"
